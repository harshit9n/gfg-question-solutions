class Solution{
public:	
	
	
	int isPalindrome(string S)
	{
	    // Your code goes here
	    int num = S.size()-1;
	    for(int i=0;i<(S.size())/2;i++)
	    {
	        if(S[i] != S[num-i])
	        {
	            return 0;
	        }
	    }
	    return 1;
	}

};
